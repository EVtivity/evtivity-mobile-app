# Active brand slug (must exist in brands/index.ts). Defaults to "default".
ACTIVE_BRAND=default

# Overrides the brand's apiUrl when set. Baked into the JS bundle at build time.
# EXPO_PUBLIC_API_URL=https://api.evtivity.com
